extends ../layout

block content

  .col-sm-6
    .panel.panel-default
      .panel-header
        h1.panel-title #{list.title}
      .panel-body

          .thumbnail.pic
            img(src='#{list.imageOne}')
            .caption
              h5 1. #{list.itemOne}


          .thumbnail.pic
            img(src='#{list.imageTwo}')
            .caption
              h5 2. #{list.itemTwo}


          .thumbnail.pic
            img(src='#{list.imageThree}')
            .caption
              h5 3. #{list.itemThree}


          .thumbnail.pic
            img(src='#{list.imageFour}')
            .caption
              h5 4. #{list.itemFour}


          .thumbnail.pic
            img(src='#{list.imageFive}')
            .caption
              h5 5. #{list.itemFive}

      .panel-footer
        .btn-group.btn-group-justified
          .btn-group
            form(action='/lists/#{list._id}?_method=DELETE', method='post')
              button.btn.btn-primary.btn-danger(type='submit') Delete
          .btn-group
            a.btn.btn-primary(href='/lists') Back
          .btn-group
            a.btn.btn-primary(href='/lists/#{list._id}/edit') Edit

  .col-sm-6
    .well
      h3 Comment On This List
      hr
      ul.media-list
        each comment in list.comments
          li.media
            .media-left
              a(href='#')
                img.media-object.avatar(src='#{comment.avatar}')
            .media-body
              h4.media-heading #{comment.postedBy} Says:
              p #{comment.text}
            hr


      include comment

